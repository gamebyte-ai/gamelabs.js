/** Default values for camera manager and controllers. */

// ── Manager ──
export const DEFAULT_ORTHO_SIZE = 10;
export const DEFAULT_FOV = 60;
export const DEFAULT_EASING = 8;
export const PERSPECTIVE_TO_ORTHO_OFFSET = 5;

// ── Front ──
export const FRONT_OFFSET = 5;

// ── Topdown ──
export const TOPDOWN_OFFSET = 10;

// ── Isometric ──
export const ISOMETRIC_DISTANCE = 15;

// ── Orbital ──
export const ORBITAL_DEFAULT_DISTANCE = 10;
export const ORBITAL_DEFAULT_AZIMUTH = 0;
export const ORBITAL_DEFAULT_PITCH = 0.3;
export const ORBITAL_DEFAULT_MIN_DISTANCE = 1;
export const ORBITAL_DEFAULT_MAX_DISTANCE = 100;
export const ORBITAL_DEFAULT_MIN_PITCH = -Math.PI / 2 + 0.01;
export const ORBITAL_DEFAULT_MAX_PITCH = Math.PI / 2 - 0.01;

// ── 2D defaults ──
export const DEFAULT_Y = 0;
export const DEFAULT_Z = 0;
