/**
 * Axial-offset coordinate of a cell in the flat-top hex grid (odd-q).
 * `col` is the column index; `row` is the row index within that column.
 */
export type HexCoord = { readonly col: number; readonly row: number };
